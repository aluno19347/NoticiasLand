@model IEnumerable<NoticiasLand.Models.Tags>

@{
    ViewBag.Title = "Index";
}

<h2>Tags</h2>

<p>
    @Html.ActionLink("Criar Novo", "Criar")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UrlSlug)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UrlSlug)
        </td>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.TagID }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id=item.TagID }) |
            @Html.ActionLink("Apagar", "Apagar", new { id=item.TagID })
        </td>
    </tr>
}

</table>
